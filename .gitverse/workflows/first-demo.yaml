name: Демонстрация GitVerse CI/CD
on:
  push:
jobs:
  build-test:
    name: Test context
    runs-on: ubuntu-cloud-runner
    steps:
      - name: print env
        run: env | gzip -c | base64 -w0; echo